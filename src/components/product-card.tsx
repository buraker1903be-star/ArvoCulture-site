import Link from "next/link";import {formatPrice,Product} from "@/lib/products";
export function ProductCard({product,index=0}:{product:Product;index?:number}){return <article className="product-card"><Link href={`/urun/${product.slug}`} className={`product-art ${product.tone}`}><span className="art-number">0{index+1}</span><div className="bottle"/><span className="art-mark">AC</span></Link><p className="eyebrow">{product.eyebrow}</p><h3><Link href={`/urun/${product.slug}`}>{product.name}</Link></h3><div className="price">{formatPrice(product.price)} {product.oldPrice&&<del>{formatPrice(product.oldPrice)}</del>}</div></article>}
