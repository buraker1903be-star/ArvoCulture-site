import {products} from "@/lib/products";import {ProductCard} from "@/components/product-card";
export default async function Collection({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const label=slug==="giyim"?"Giyim":slug==="bakim"?"Kişisel Bakım":slug==="parfum"?"Parfüm":"Tüm Ürünler";const list=slug==="tumu"?products:products.filter(p=>p.category.toLocaleLowerCase("tr-TR").includes(label.toLocaleLowerCase("tr-TR").split(" ")[0]));return <main><section className="collection-hero"><p className="eyebrow">ARVOCULTURE SEÇKİSİ</p><h1>{label}</h1><p>Kendine ait olanı keşfet. Her ürün; tasarım, nitelik ve kullanım deneyimi gözetilerek seçildi.</p></section><section className="featured"><div className="filterbar"><span>{list.length} ürün</span><span>Önerilen sıralama ↓</span></div><div className="product-grid">{list.map((p,i)=><ProductCard key={p.slug} product={p} index={i}/>)}</div></section></main>}
