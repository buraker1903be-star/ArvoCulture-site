import type {CSSProperties} from "react";import type {Metadata} from "next";import Image from "next/image";import "./globals.css";import {CartProvider} from "@/components/cart";import {Header} from "@/components/header";import {getStorefrontTheme} from "@/lib/storefront-theme";
export async function generateMetadata():Promise<Metadata>{const theme=await getStorefrontTheme();return {title:{default:`${theme.store_name??"ArvoCulture"} — Seçtiğin Şey Sensin`,template:`%s | ${theme.store_name??"ArvoCulture"}`},description:theme.hero_description,icons:theme.favicon_url?{icon:theme.favicon_url}:undefined};}
export default async function RootLayout({children}:{children:React.ReactNode}){const theme=await getStorefrontTheme();const style={"--ink":theme.primary_color,"--acid":theme.accent_color,"--paper":theme.background_color,"--products-per-row":theme.products_per_row} as CSSProperties;return <html lang="tr"><body style={style} className={`theme-${theme.typography} hero-${theme.hero_style} header-${theme.header_layout} ${theme.sticky_header?"header-sticky":"header-static"} cards-${theme.product_card_style} ratio-${theme.product_image_ratio}`}><CartProvider><Header theme={theme}/>{children}<footer><div className="logo light">{theme.logo_url?<Image src={theme.logo_url} alt={theme.store_name??"ArvoCulture"} width={220} height={60}/>:<><b>ARVO</b><i>CULTURE</i></>}</div><p>{theme.footer_tagline}</p><div><a href="mailto:hello@arvoculture.com">İletişim</a><a href="#">Teslimat & İade</a>{theme.instagram_url&&<a href={theme.instagram_url}>Instagram</a>}{theme.facebook_url&&<a href={theme.facebook_url}>Facebook</a>}</div><small>© 2026 {theme.store_name??"ArvoCulture"}. Tüm hakları saklıdır.</small></footer></CartProvider></body></html>}
